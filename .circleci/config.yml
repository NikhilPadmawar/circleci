jobs:
  build:
    docker:
      - image: circleci/node:14.15.1

    working_directory: ~/repo
        
    steps:
      - checkout
      - run: echo "this is the build job"
      - run: npm install
      - run: npm test
  